#mode "ENGLISH"
LET X = 1
WHILE X <= 10 THEN
    IF X % 2 == 0 THEN
        PRINT X
        PRINT " is even"
    ELSE
        PRINT X
        PRINT " is odd"
    END
    IF X == 5 THEN
        PRINT "Halfway there!"
    END
    LET X = X + 1
WEND
PRINT "Done!"